<core:FragmentDefinition xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:core="sap.ui.core">
    <Dialog title="{i18n>OrderDetail.TecoStatusRemove}" type="Message">
        <content>
            <l:VerticalLayout width="100%">
                <Text text="{i18n>OrderDetail.TecoStatusRemoveMessage}"/>
            </l:VerticalLayout>
        </content>
        <buttons>
            <Button text="{i18n>General.Cancel}" press="onRemoveTecoFlagCancel"/>
            <Button text="{i18n>General.Save}" press="onRemoveTecoFlagSave"/>
        </buttons>
    </Dialog>
</core:FragmentDefinition>